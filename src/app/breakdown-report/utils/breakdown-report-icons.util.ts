import { addIcons } from 'ionicons';
import { cameraOutline, cloudUploadOutline, imageOutline, locationOutline } from 'ionicons/icons';

export function registerBreakdownReportIcons(): void {
  addIcons({ cameraOutline, locationOutline, imageOutline, cloudUploadOutline });
}
