import { addIcons } from 'ionicons';
import { cameraOutline, cloudUploadOutline, filmOutline, imageOutline, locationOutline, videocamOutline } from 'ionicons/icons';

export function registerBreakdownReportIcons(): void {
  addIcons({ cameraOutline, locationOutline, imageOutline, cloudUploadOutline, videocamOutline, filmOutline });
}
